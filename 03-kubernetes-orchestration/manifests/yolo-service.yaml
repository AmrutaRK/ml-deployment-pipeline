apiVersion: v1
kind: Service
metadata:
  name: yolo-detection-service
spec:
  type: LoadBalancer   # a SEPARATE external IP from resnet18-service -
                        # each Service gets its own address by default
  selector:
    app: yolo-detection
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
